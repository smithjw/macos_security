id: sysprefs_speak_typing_feedback_disable
title: "Disable Speak Typing Feedback"
discussion: |
  Speak typing feedback should be disabled if it is not needed.

  Disabling speak typing feedback ensures the system does not audibly expose potentially sensitive data.
  
  [NOTE] 
  ====
  The check and fix are for the currently logged in user. To get the currently logged in user, run the following.
  [source,bash]
  ----
  CURRENT_USER=$( scutil <<< "show State:/Users/ConsoleUser" | awk '/Name :/ && ! /loginwindow/ { print $3 }' )
  ----
  ====
check: |
  /usr/bin/sudo -u "$CURRENT_USER" /usr/bin/defaults read com.apple.universalaccess.plist typingEchoEnabled
result:
  boolean: 0
fix: |
  [source,bash]
  ----
  /usr/bin/sudo -u "$CURRENT_USER" /usr/bin/defaults write com.apple.universalaccess typingEchoEnabled -bool false
  ----
references:
  cce:
    - N/A
  cci: 
    - N/A
  800-53r4: 
    - CM-2
    - CM-7
  srg: 
    - N/A
  disa_stig: 
    - N/A
  800-171r2:
    - N/A
macOS:
  - "11.0"
tags:
  - 800-179_sslf    
  - preliminary
mobileconfig: false
mobileconfig_info:
